* 阻塞OS锁
	* mutex
* 自旋锁
	* atomic（需要操作系统支持原子操作
	* 死循环里不断检查
* 读写锁

#### 索引并发方法
* 分段hash表  —— page latches
	* 把hash表分段 ， 每个段单独加锁
	* 支持并发
	* java的concurrent hash 就是如此
* 分槽hash表—— slot latches
	* 按槽加锁
	* 更细，但维护更难
